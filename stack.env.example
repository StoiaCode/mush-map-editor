# Copy this file to stack.env (kept out of git) and fill in a real value.
# Generate one with: openssl rand -hex 24
# Letters/digits only - it gets sed-substituted into js/sync.js at container start.
CLIENT_KEY=
